@model IEnumerable<Domain.Model.Entities.GroupEntity>

@{
    ViewData["Title"] = "Index";
}

    <section class="content-header">
        <h1>Index</h1>
        <form asp-action="AddAdminClaim" asp-controller="Identity">
            <div class="form-group">
                <label name="email" class="control-label">Email</label>
                <input name="email" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Add Claim" class="btn btn-primary" />
            </div>
        </form>
    </section>

<section class="content container-fluid">
    <hr />
    <div class="row">
        <div class="col-lg-8 col-md-6 col-sm-12">
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.GroupName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.MusicalGenre)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Beginnings)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.City)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Nation)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.BandMascot)
                        </th>
                        <th>
                            Action
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.GroupName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MusicalGenre)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Beginnings)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.City)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nation)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BandMascot)
                        </td>
                        <td>
                            <button type="button" class="btn btn-warning">
                                <i class="far fa-edit">

                                </i>
                                @Html.ActionLink("Edit", "Edit", new { Id = item.GroupId })
                            </button>
                            <button type="button" class="btn btn-info">
                                <i class="fas fa-eye">

                                </i>
                                @Html.ActionLink("Details", "Details", new { Id = item.GroupId })
                            </button>
                            <button type="button" class="btn btn-danger">
                                <i class="far fa-trash-alt">

                                </i>
                                @Html.ActionLink("Delete", "Delete", new { Id = item.GroupId })
                            </button>
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>

